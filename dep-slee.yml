apiVersion: apps/v1
kind : Deployment
metadata:
  name: dep-resource
  namespace: prod
  annotations:
    kubernetes.io/change-cause: "slee-image"
spec:
  minReadySeconds: 5
  replicas: 3
  selector:
    matchExpressions: 
      - key: web
        operator: In
        values: 
          - webexpre
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      name: dep-pod
      labels:
        web: webexpre
    spec:
      containers:
        - name: dep-cont
          image: srinuparella/cssi:slee
          # ports:
          #   - containerPort: 80
          #     protocol: TCP
          startupProbe:
            httpGet:
              path: /
              port: 80
          livenessProbe:
            httpGet:
              path: /
              port: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
          resources:
            requests:
              cpu: "200m"
              memory: "350Mi"
            limits:
              cpu: "500m" 
              memory: "1000Mi"


